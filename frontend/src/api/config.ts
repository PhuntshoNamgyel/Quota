// src/api/config.ts
// On a physical phone via Expo Go, localhost = the phone, NOT your Mac.
// Use your Mac's LAN IP so the phone can reach the backend over WiFi.
export const API_URL = 'http://10.2.26.44:4000'; // <-- Mac's IP